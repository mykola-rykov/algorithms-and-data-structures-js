/**
 * Traverse a tree in a Depth First, Pre Order way
 * @param root tree root node
 * @returns tree values flattened into an array
 */
const dfsPreOrder = (root) => {
    let treeValues = [];

    return treeValues;
};

/**
 * Traverse a tree in a Depth First, Post Order way
 * @param root tree root node
 * @returns tree values flattened into an array
 */
const dfsPostOrder = (root) => {
    let treeValues = [];

    return treeValues;
};

/**
 * Traverse a binary tree in a Depth First, In Order way
 * @param root tree root node
 * @returns tree values flattened into an array
 */
const dfsInOrder = (root) => {
    let treeValues = [];

    return treeValues;
};

export { dfsPreOrder, dfsPostOrder, dfsInOrder };
