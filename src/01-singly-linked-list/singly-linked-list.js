class SinglyLinkedListNode {
    constructor(value, next = null) {
        this.value = value;
        this.next = next;
    }
}

class SinglyLinkedList {
    constructor() {
        
    }

    /**
     * Appends a new node to the end of a linked list
     * @param value 
     * @returns a new list with the value appended to the end of it
     */
    append(value) {
        
    }

    /**
     * Removes a first node with a given value from a linked list
     * @param value
     * @returns removed node or null if there is no such value in the list
     */
    remove(value) {
        
    }

    /**
     * Finds a first node with a given value in a linked list
     * @param value 
     * @returns found node or null if there is no such value in the list
     */
    find(value) {
        
    }

    /**
     * Reverses a linked list in-place
     */
    reverse() {

    }
}

export default SinglyLinkedList;
