class MaxHeap {
    constructor() {
        this.values = [];
    }

    /**
     * Inserts given value to a correct spot in a Binary Heap.
     * @param value 
     */
    insert(value) {
        
    }

    /**
     * Allows to extract Binary Heap's max value.
     * Rebuilds Binary Heap with a second max value as a new root.
     * @returns Binary Heap's max value
     */
    extractMax() {
        
    }
}

export default MaxHeap;
