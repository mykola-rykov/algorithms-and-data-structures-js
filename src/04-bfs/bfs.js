/**
 * Traverses a tree in Breadth-First way
 * @param root tree root node
 * @returns tree values flattened into an array
 */
const bfs = (root) => {
    let treeValues = [];

    return treeValues;
};

export default bfs;
