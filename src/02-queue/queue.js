class Queue {
    constructor() {
    }

    /**
     * Returns true if a queue is empty and false otherwise
     */
    isEmpty() {
        
    }

    /**
     * Returns first value in a queue without dequeuing it
     */
    peek() {
        
    }

    /**
     * Returns a queue with a value added to the end of it
     * @param value 
     */
    enqueue(value) {

    }

    /**
     * Returns first value in a queue
     */
    dequeue() {
        
    }
}

export default Queue;
