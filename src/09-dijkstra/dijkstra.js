class WeightedGraph {
    constructor() {
        this.adjacencyList = {};
    }

    /**
     * Adds a vertex to the weighted graph
     * @param vertex 
     */
    addVertex(vertex) {
        
    }

    /**
     * Adds an edge to the weighted graph
     * @param vertex 
     */
    addEdge(vertex1, vertex2, weight) {
        
    }

    /**
     * Finds a shortest distance between `vertex1` and `vertex2`
     * using Dijkstra's shortest path algo
     * @param vertex1 
     * @param vertex2 
     */
    Dijkstra(vertex1, vertex2) {
        
    }
}

export default WeightedGraph;
