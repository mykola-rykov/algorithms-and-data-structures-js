class BinarySearchTreeNode {
    constructor(value = null) {
        this.value = value;
        this.left = null;
        this.right = null;
    }

    /**
     * Inserts a value to a BST 
     * @param value 
     * @returns BST node which contains inserted value
     */
    insert(value) {
        
    }

    /**
     * Finds a node with a given value
     * @param value 
     * @returns BST node with a given value or null if it not exists
     */
    find(value) {
        
    }
}

export default BinarySearchTreeNode;
